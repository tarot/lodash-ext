{
  "name": "lodash-ext",
  "version": "1.0.0",
  "description": "",
  "main": "lib/index.js",
  "scripts": {
    "build": "rm -rf lib && mkdir -p lib && babel src --out-dir lib",
    "prepare": "npm run build",
    "test": "mocha --require babel-register test/**/*.spec.js"
  },
  "author": "",
  "license": "MIT",
  "files": [
    "lib"
  ],
  "peerDependencies": {
    "lodash": "^4.17.4"
  },
  "devDependencies": {
    "babel-cli": "^6.26.0",
    "babel-plugin-espower": "^2.3.2",
    "babel-preset-env": "^1.6.1",
    "lodash": "^4.17.4",
    "mocha": "^4.0.1",
    "power-assert": "^1.4.4"
  }
}
